import {
  account,
  invitation,
  organization,
  organizationMembership,
  session,
  user,
  verification,
} from "@/features/auth/db"

export const authSchema = {
  account,
  invitation,
  organization,
  organizationMembership,
  session,
  user,
  verification,
}
