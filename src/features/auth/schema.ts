import {
  account,
  apikey,
  invitation,
  organization,
  organizationMembership,
  session,
  user,
  verification,
} from "@/features/auth/db"

export const authSchema = {
  account,
  apikey,
  invitation,
  organization,
  organizationMembership,
  session,
  user,
  verification,
}
