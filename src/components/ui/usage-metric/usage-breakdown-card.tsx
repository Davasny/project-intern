import type { HTMLAttributes } from "react"
import { SectionCard } from "@/components/ui/section-card/section-card"

export const UsageBreakdownCard = (props: HTMLAttributes<HTMLDivElement>) => (
  <SectionCard {...props} />
)
